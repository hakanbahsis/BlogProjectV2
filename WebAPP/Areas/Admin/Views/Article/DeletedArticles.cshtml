@model List<ArticleDto>
@{
    ViewData["Title"] = "DeletedArticles";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}


<div class="container-xxl flex-grow-1 container">
    <h4 class="fw-bold py-3"><span class="text-muted fw-light">Çöp Kutusu /</span> Makaleler</h4>
</div>

<div class="card">
    @* <h5 class="card-header">Table Basic</h5>*@
 
    <div class="table-responsive text-nowrap m-5">
        <table class="table table-bordered" id="articlesTable" width="100%" cellpadding="0">
            <thead>
                <tr>
                    <th>Başlık</th>
                    <th>Kategori</th>
                    <th>Oluşturulma Tarihi</th>
                    <th>Oluşturan</th>
                    <th>Durum</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tfoot>
                <tr>
                    <th>Başlık</th>
                    <th>Kategori</th>
                    <th>Oluşturulma Tarihi</th>
                    <th>Oluşturan</th>
                    <th>Durum</th>
                    <th>İşlemler</th>
                </tr>
            </tfoot>
            <tbody class="table-border-bottom-0">
                @foreach (var item in Model)
                {
                    <tr name="@item.Id">
                        <th>@item.Title</th>
                        <th>@item.Category.Name</th>
                        <th>@item.CreatedDate.ToShortDateString()</th>
                        <th>@item.CreatedBy</th>
                        <th>@(item.IsDeleted ? "Silinmiş" : "Aktif")</th>
                        <th>
                            <a class="btn btn-sm btn-warning" data-id="@item.Id" asp-area="Admin" asp-controller="Article" asp-action="UndoDelete" asp-route-articleId="@item.Id"><i class='bx bx-undo'></i></a>
                           @* <a class="btn btn-sm btn-danger" data-id="@item.Id" asp-area="Admin" asp-controller="Article" asp-action="Delete" asp-route-articleId="@item.Id"><i class="bx bx-minus-circle"></i></a>*@
                        </th>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts{
    <script src="~/js/articleIndex.js"></script>
}
