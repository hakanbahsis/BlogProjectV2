@model List<CategoryDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}


<div class="container-xxl flex-grow-1 container">
    <h4 class="fw-bold py-3"><span class="text-muted fw-light">Kategori Yönetimi /</span> Kategoriler</h4>
</div>

<div class="card">
    @* <h5 class="card-header">Table Basic</h5>*@
    <div class="col-3 m-1">

        <a class="btn btn-success" asp-area="Admin" asp-controller="Category" asp-action="Add">Kategori Ekle <i class="bx bx-plus-medical"></i></a>
    </div>
    <div class="table-responsive text-nowrap m-5">
        <table class="table table-bordered" id="categoriesTable" width="100%" cellpadding="0">
            <thead>
                <tr>
                    <th>Kategori Adı</th>
                    <th>Oluşturulma Tarihi</th>
                    <th>Oluşturan</th>
                    <th>Durum</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tfoot>
                <tr>
                    <th>Kategori Adı</th>
                    <th>Oluşturulma Tarihi</th>
                    <th>Oluşturan</th>
                    <th>Durum</th>
                    <th>İşlemler</th>
                </tr>
            </tfoot>
            <tbody class="table-border-bottom-0">
                @foreach (var item in Model)
                {
                    <tr name="@item.Id">
                        <th>@item.Name</th>
                        <th>@item.CreatedDate.ToShortDateString()</th>
                        <th>@item.CreatedBy</th>
                        <th>@(item.IsDeleted ? "Silinmiş" : "Aktif")</th>
                        <th>
                            <a class="btn btn-sm btn-warning" data-id="@item.Id" asp-area="Admin" asp-controller="Category" asp-action="Update" asp-route-categoryId="@item.Id"><i class="bx bx-edit"></i></a>
                            <a class="btn btn-sm btn-danger" data-id="@item.Id" asp-area="Admin" asp-controller="Category" asp-action="Delete" asp-route-categoryId="@item.Id"><i class="bx bx-minus-circle"></i></a>
                        </th>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts{
    <script src="~/js/categoryIndex.js"></script>
}
