

@model List<ArticleDto>
@{
    ViewData["Title"] = "Home Page";
}

@foreach(var item in Model)
{
    <h1>@item.Title</h1><br />
@*    <h3>@item.Content</h3>
*@    <h3>@item.CreatedBy</h3>
    <br />
    <br />
}